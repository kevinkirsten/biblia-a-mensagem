import { Text, View } from "react-native";

export default function About() {
  return (
    <View className="flex-1 bg-sky-500">
      <Text className="text-zinc-100 text-xl font-heading">Sobre</Text>
    </View>
  );
}
